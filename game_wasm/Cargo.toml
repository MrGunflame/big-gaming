[package]
name = "game_wasm"
version = "0.1.0"
edition = "2021"

[features]
default = []
raw = []

[dependencies]
# FIXME: Remove render dependencies from common.
# game_common = { version = "0.1.0", path = "../game_common" }
