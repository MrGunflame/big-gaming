[package]
name = "move_forward"
version = "0.1.0"
edition = "2021"

[dependencies]
game_wasm = { version = "0.1.0", path = "../../../../game_wasm" }
shared = { version = "0.1.0", path = "../shared" }
